import { ShowExonsDirective } from './show-exons.directive';

describe('ShowExonsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowExonsDirective();
    expect(directive).toBeTruthy();
  });
});
